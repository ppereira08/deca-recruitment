{
  "name": "deca-recruitment",
  "version": "1.0.0",
  "description": "Recruitment website for a high school DECA chapter with a customizable interest form",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env.local server.js",
    "init-db": "node --env-file-if-exists=.env.local scripts/init-db.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
